name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Self tests
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Run checks
        run: swift run SelfTest
